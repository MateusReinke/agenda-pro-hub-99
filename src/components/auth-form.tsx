'use client';
import { useActionState } from 'react';
import { Button } from './ui/button';
import { Input } from './ui/input';
import { Field } from './form-fields';
export function LoginForm({ action }: { action: (prev: unknown, fd: FormData) => Promise<{error?: string}|never> }) { const [state, formAction, pending]=useActionState(action,{}); return <form action={formAction} className="grid gap-4"><Field label="E-mail"><Input name="email" type="email" required defaultValue="admin@demo.com"/></Field><Field label="Senha"><Input name="password" type="password" required defaultValue="123456"/></Field>{state?.error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}<Button disabled={pending}>{pending?'Entrando...':'Entrar'}</Button></form>; }
export function RegisterForm({ action }: { action: (prev: unknown, fd: FormData) => Promise<{error?: string}|never> }) { const [state, formAction, pending]=useActionState(action,{}); return <form action={formAction} className="grid gap-4"><Field label="Nome do salão"><Input name="salonName" required/></Field><div className="grid gap-4 md:grid-cols-2"><Field label="Seu nome"><Input name="name" required/></Field><Field label="E-mail"><Input name="email" type="email" required/></Field></div><Field label="Senha"><Input name="password" type="password" minLength={6} required/></Field><div className="grid gap-4 md:grid-cols-3"><Field label="WhatsApp"><Input name="whatsapp"/></Field><Field label="Cidade"><Input name="city"/></Field><Field label="Estado"><Input name="state" maxLength={2}/></Field></div>{state?.error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}<Button disabled={pending}>{pending?'Criando...':'Criar salão'}</Button></form>; }
