import { prisma } from '@/lib/prisma';
import { requireUser } from '@/server/auth';
import { upsertClient, deleteClient } from '@/server/actions';
import { Card } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Textarea } from '@/components/ui/textarea'; import { Button } from '@/components/ui/button'; import { DataTable, Th, Td } from '@/components/ui/data-table'; import { Field } from '@/components/form-fields';
export default async function ClientsPage(){ const u=await requireUser(); const clients=await prisma.client.findMany({where:{salonId:u.salonId},include:{appointments:{include:{service:true},orderBy:{date:'desc'},take:3}},orderBy:{createdAt:'desc'}}); return <div className="space-y-6"><h2 className="text-3xl font-black">Clientes</h2><Card><h3 className="mb-4 font-bold">Novo cliente</h3><form action={upsertClient} className="grid gap-3 md:grid-cols-4"><Field label="Nome"><Input name="name" required/></Field><Field label="Telefone"><Input name="phone" required/></Field><Field label="E-mail"><Input name="email" type="email"/></Field><Field label="Observações"><Textarea name="notes" rows={1}/></Field><Button className="md:col-span-4">Salvar cliente</Button></form></Card><DataTable><thead><tr><Th>Cliente</Th><Th>Contato</Th><Th>Histórico</Th><Th>Ações</Th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><Td><strong>{c.name}</strong><form action={upsertClient} className="mt-2 grid gap-2"><input type="hidden" name="id" value={c.id}/><Input name="name" defaultValue={c.name}/><Input name="phone" defaultValue={c.phone}/><Input name="email" defaultValue={c.email||''}/><Button variant="secondary">Editar</Button></form></Td><Td>{c.phone}<br/>{c.email}</Td><Td>{c.appointments.map(a=><p key={a.id}>{a.service.name} • {a.status}</p>)}</Td><Td><form action={deleteClient}><input type="hidden" name="id" value={c.id}/><Button variant="danger">Excluir</Button></form></Td></tr>)}</tbody></DataTable></div> }
