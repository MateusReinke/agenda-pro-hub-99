import { prisma } from '@/lib/prisma'; import { money } from '@/lib/utils'; import { requireUser } from '@/server/auth'; import { toggleService, upsertService } from '@/server/actions'; import { Card } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Textarea } from '@/components/ui/textarea'; import { Button } from '@/components/ui/button'; import { DataTable, Th, Td } from '@/components/ui/data-table'; import { Badge } from '@/components/ui/badge'; import { Field } from '@/components/form-fields';
export default async function ServicesPage(){ const u=await requireUser(); const items=await prisma.service.findMany({where:{salonId:u.salonId},orderBy:{createdAt:'desc'}}); return <div className="space-y-6"><h2 className="text-3xl font-black">Serviços</h2><Card><form action={upsertService} className="grid gap-3 md:grid-cols-4"><Field label="Nome"><Input name="name" required/></Field><Field label="Preço"><Input name="price" type="number" step="0.01" required/></Field><Field label="Duração (min)"><Input name="durationMinutes" type="number" required/></Field><Field label="Descrição"><Textarea name="description" rows={1}/></Field><Button className="md:col-span-4">Salvar serviço</Button></form></Card><DataTable><thead><tr><Th>Serviço</Th><Th>Preço</Th><Th>Duração</Th><Th>Status</Th><Th>Ações</Th></tr></thead><tbody>{items.map(s=><tr key={s.id}><Td><form action={upsertService} className="grid gap-2"><input name="id" type="hidden" value={s.id}/><Input name="name" defaultValue={s.name}/><Textarea name="description" defaultValue={s.description||''}/><Input name="price" type="number" step="0.01" defaultValue={String(s.price)}/><Input name="durationMinutes" type="number" defaultValue={s.durationMinutes}/><Button variant="secondary">Editar</Button></form></Td><Td>{money(s.price)}</Td><Td>{s.durationMinutes} min</Td><Td><Badge>{s.active?'Ativo':'Inativo'}</Badge></Td><Td><form action={toggleService}><input type="hidden" name="id" value={s.id}/><input type="hidden" name="active" value={String(!s.active)}/><Button variant="secondary">{s.active?'Desativar':'Ativar'}</Button></form></Td></tr>)}</tbody></DataTable></div> }
