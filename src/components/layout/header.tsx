import Link from 'next/link';
import { getSessionUser } from '@/server/auth';
export async function Header() { const user = await getSessionUser(); return <header className="flex flex-wrap items-center justify-between gap-3 border-b border-rose-100 bg-white/70 px-4 py-4 backdrop-blur md:px-8"><div><p className="text-sm text-slate-500">Salão</p><h1 className="text-xl font-bold text-slate-900">{user?.salon.name}</h1></div><div className="flex items-center gap-3 text-sm"><Link className="rounded-full bg-rose-50 px-3 py-2 text-rose-700" href={`/public/${user?.salon.slug}`}>Link público</Link><span className="hidden md:inline">{user?.name}</span><form action="/api/auth/logout" method="post"><button className="rounded-full border border-rose-100 px-3 py-2">Sair</button></form></div></header>; }
